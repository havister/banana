from django.urls import path
#from signals.views import IndexView, SignalView

app_name = 'signals'

urlpatterns = [
    # /signals/
    #path('', IndexView.as_view(), name='index'),
    # /signals/signal_id
    #path('<int:pk>/', SignalView.as_view(), name='signal'),
]
